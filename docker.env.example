# Docker 部署用環境變數
# 複製為 .env 並填入實際值，放在專案根目錄
#
# 部署步驟：
# 1. cp docker.env.example .env 並填入實際值
# 2. docker compose build
# 3. docker compose up -d
# 4. 對外存取：http://VM_IP (port 80)
#    HTTPS：在 VM 上用 readyqa-https.conf + Certbot，proxy 到 localhost:80

# ===== 前端 Build 時需要（NEXT_PUBLIC_* 會編進 bundle）=====
NEXT_PUBLIC_API_URL=https://readyqa.crossbot.com.tw/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
NEXT_PUBLIC_APP_URL=https://readyqa.crossbot.com.tw

# ===== 後端執行時需要 =====
# DATABASE_URL、ELASTICSEARCH_HOST 由 docker-compose 自動設定
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# Elasticsearch（docker-compose 會覆寫 HOST，其他可選）
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=admin123456
EMBEDDING_DIMENSIONS=3072

# Stripe
STRIPE_SECRET_KEY=sk_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_ID_STARTER=price_xxx
STRIPE_PRICE_ID_PRO=price_xxx
STRIPE_PRICE_ID_ENTERPRISE=price_xxx
FRONTEND_URL=https://readyqa.crossbot.com.tw

# GCS（可選）
# GCS_BUCKET=your-bucket-name
