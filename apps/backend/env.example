# Backend Environment Variables
# 複製此檔為 .env 並填入實際值（backend 使用 .env，與 VM 一致）

# Server
PORT=8000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://qaplus:password@localhost:5432/qaplus?schema=public"

# Supabase (與前端共用 NEXT_PUBLIC_ 變數)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-supabase-anon-key
# 或使用前端變數：
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# Elasticsearch (可選，如果未配置則 ES 功能會被禁用)
ELASTICSEARCH_HOST=http://localhost:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=admin123456
# ELASTICSEARCH_API_KEY=your-api-key  # 優先使用 API Key（如果設置）
EMBEDDING_DIMENSIONS=3072  # Dense 向量維度（默認 3072）

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=60

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret
STRIPE_PRICE_ID_STARTER=price_your_starter_price_id
STRIPE_PRICE_ID_PRO=price_your_pro_price_id
STRIPE_PRICE_ID_ENTERPRISE=price_your_enterprise_price_id

# Frontend URL (for Stripe redirects)
# 開發: FRONTEND_URL=http://localhost:3000
# 正式（無 HTTPS）: FRONTEND_URL=http://readyqa.crossbot.com.tw
# 正式（有 HTTPS）: FRONTEND_URL=https://readyqa.crossbot.com.tw
FRONTEND_URL=http://localhost:3000

# 前端 NEXT_PUBLIC_API_URL 說明：
# - 無 nginx：後端在 8000 → http://readyqa.crossbot.com.tw:8000/api
# - 有 nginx 代理 /api：http://readyqa.crossbot.com.tw/api

# Google Cloud Storage（可選，設定後 Logo/首頁圖上傳至 GCS，未設定則存本機 uploads/）
# GCS_BUCKET=2026_reas_readyqa_dev
# 認證：VM 上可用預設服務帳戶；本機請設 GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

